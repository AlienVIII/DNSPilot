<Window
    x:Class="DNSPilotWindows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="DNS Pilot">

    <Grid Padding="24" RowSpacing="16" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Text="DNS Pilot" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Windows Store-safe DNS benchmark shell" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Click="QuickBenchmark_Click" ToolTipService.ToolTip="Run the default DNS + TCP benchmark command.">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Play" />
                        <TextBlock Text="Quick benchmark" />
                    </StackPanel>
                </Button>
                <Button Click="ValidateSystemDns_Click" ToolTipService.ToolTip="Benchmark the current Windows resolver path.">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Refresh" />
                        <TextBlock Text="Validate DNS" />
                    </StackPanel>
                </Button>
                <Button Click="OpenSettings_Click" ToolTipService.ToolTip="Open Windows Network settings without changing DNS.">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Setting" />
                        <TextBlock Text="Settings" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <NavigationView
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            PaneDisplayMode="Top"
            IsSettingsVisible="False"
            SelectionChanged="SectionNav_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Benchmark" Tag="Benchmark" Icon="Play" IsSelected="True" />
                <NavigationViewItem Content="Apply" Tag="Apply" Icon="Copy" />
                <NavigationViewItem Content="Profiles" Tag="Profiles" Icon="Edit" />
                <NavigationViewItem Content="History" Tag="History" Icon="Clock" />
                <NavigationViewItem Content="Diagnostics" Tag="Diagnostics" Icon="ReportHacked" />
            </NavigationView.MenuItems>
        </NavigationView>

        <ScrollViewer Grid.Row="2">
            <Grid ColumnSpacing="16" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*" />
                    <ColumnDefinition Width="0.9*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border x:Name="BenchmarkSection" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="14">
                        <TextBlock Text="Benchmark" FontSize="20" FontWeight="SemiBold" />
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="ModeCombo" Header="Mode" SelectedIndex="1" ToolTipService.ToolTip="DNS only, DNS + TCP, or current/system DNS validation.">
                                <ComboBoxItem Content="DNS only" />
                                <ComboBoxItem Content="DNS + TCP" />
                                <ComboBoxItem Content="System DNS validation" />
                            </ComboBox>
                            <ComboBox x:Name="RecordFamilyCombo" Grid.Column="1" Header="Record family" SelectedIndex="0" ToolTipService.ToolTip="A returns IPv4 answers. AAAA returns IPv6 answers. Use A only when IPv6 is weak.">
                                <ComboBoxItem Content="A + AAAA" />
                                <ComboBoxItem Content="A only" />
                                <ComboBoxItem Content="AAAA only" />
                            </ComboBox>
                            <ComboBox x:Name="ResolverFamilyCombo" Grid.Column="2" Header="Resolver address" SelectedIndex="0" ToolTipService.ToolTip="Choose Auto, IPv4 DNS servers only, or IPv6 DNS servers only.">
                                <ComboBoxItem Content="Auto" />
                                <ComboBoxItem Content="IPv4" />
                                <ComboBoxItem Content="IPv6" />
                            </ComboBox>
                        </Grid>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="AttemptsBox" Header="Attempts" Value="2" Minimum="1" />
                            <NumberBox x:Name="DnsTimeoutBox" Grid.Column="1" Header="DNS timeout ms" Value="800" Minimum="1" />
                            <NumberBox x:Name="TcpTimeoutBox" Grid.Column="2" Header="TCP timeout ms" Value="1000" Minimum="1" />
                            <NumberBox x:Name="TcpTargetsBox" Grid.Column="3" Header="TCP targets/domain" Value="4" Minimum="1" />
                        </Grid>

                        <TextBlock Text="Command preview" FontWeight="SemiBold" />
                        <TextBox x:Name="CommandPreviewBox" IsReadOnly="True" TextWrapping="Wrap" MinHeight="76" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Click="QuickBenchmark_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Play" />
                                    <TextBlock Text="Run benchmark" />
                                </StackPanel>
                            </Button>
                            <Button Click="CopyCommand_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Copy" />
                                    <TextBlock Text="Copy command" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border x:Name="ProcessSection" Grid.Column="1" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="14">
                        <TextBlock Text="Process" FontSize="20" FontWeight="SemiBold" />
                        <ListView x:Name="StepsList" Header="Steps" MaxHeight="220" />
                        <ListView x:Name="ResolversList" Header="Resolvers" MaxHeight="220" />
                    </StackPanel>
                </Border>

                <Border x:Name="ApplySection" Grid.Row="1" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="14">
                        <TextBlock Text="Apply guidance" FontSize="20" FontWeight="SemiBold" />
                        <TextBox x:Name="DnsServersBox" Header="DNS servers" IsReadOnly="True" TextWrapping="Wrap" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Click="CopyDnsServers_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Copy" />
                                    <TextBlock Text="Copy DNS" />
                                </StackPanel>
                            </Button>
                            <Button Click="OpenSettings_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Setting" />
                                    <TextBlock Text="Open settings" />
                                </StackPanel>
                            </Button>
                            <Button Click="CopyChecklist_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="List" />
                                    <TextBlock Text="Copy checklist" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <TextBox x:Name="ChecklistBox" Header="Checklist" IsReadOnly="True" TextWrapping="Wrap" MinHeight="120" />
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1" Grid.Column="1" RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Border x:Name="ProfilesSection" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Custom DNS profiles" FontSize="20" FontWeight="SemiBold" />
                            <ListView x:Name="ProfilesList" Header="Saved profiles" MaxHeight="160" SelectionChanged="ProfilesList_SelectionChanged" />
                            <TextBox x:Name="ProfileNameBox" Header="Name" Text="Cloudflare Lab" />
                            <TextBox x:Name="ProfileIdBox" Header="Profile ID for update/delete" Text="cloudflare-lab" />
                            <TextBox x:Name="Ipv4Box" Header="IPv4 servers" Text="1.1.1.1, 1.0.0.1" ToolTipService.ToolTip="Plain IPv4 DNS server addresses." />
                            <TextBox x:Name="Ipv6Box" Header="IPv6 servers" Text="2606:4700:4700::1111" ToolTipService.ToolTip="Plain IPv6 DNS server addresses." />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Click="PreviewProfileSave_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="PreviewLink" />
                                        <TextBlock Text="Preview" />
                                    </StackPanel>
                                </Button>
                                <Button Click="SaveProfile_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="Save" />
                                        <TextBlock Text="Add" />
                                    </StackPanel>
                                </Button>
                                <Button Click="UpdateProfile_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="Edit" />
                                        <TextBlock Text="Update" />
                                    </StackPanel>
                                </Button>
                                <Button Click="DeleteProfile_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="Delete" />
                                        <TextBlock Text="Delete" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border x:Name="DiagnosticsSection" Grid.Row="1" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Diagnostics" FontSize="20" FontWeight="SemiBold" />
                            <ListView x:Name="HistoryList" Header="History" MaxHeight="160" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Click="RefreshStorage_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="Refresh" />
                                        <TextBlock Text="Refresh" />
                                    </StackPanel>
                                </Button>
                                <Button Click="ClearHistory_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="Delete" />
                                        <TextBlock Text="Clear history" />
                                    </StackPanel>
                                </Button>
                                <Button Click="DeleteHistory_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <SymbolIcon Symbol="Remove" />
                                        <TextBlock Text="Delete selected" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <TextBox x:Name="DiagnosticsBox" Header="Report" IsReadOnly="True" TextWrapping="Wrap" MinHeight="160" />
                            <Button Click="CopyDiagnostics_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Copy" />
                                    <TextBlock Text="Copy report" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
