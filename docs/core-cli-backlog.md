# Core CLI Backlog

## Open Requests
- Stabilize benchmark progress event schema across DNS-only, DNS+TCP, and system-DNS validation.
- Add machine-readable failure step and reason fields for all benchmark modes.
- Add system-DNS validation output compatible with app result decoders.
- Add optional flush instruction payload per platform.
- Add richer resolver/provider metadata sources and provenance fields.

## From macOS Lane
- Keep apply-plan payload authoritative for manual apply guidance.
- Preserve tested resolver in apply-plan.
- Expose safe post-apply validation command.

## From Future Lanes
- Mobile: profile/settings enablement contracts.
- Windows: settings URI and admin-service capability contract.
- Linux: NetworkManager/systemd-resolved/polkit capability detection contract.

